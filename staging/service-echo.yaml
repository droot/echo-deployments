apiVersion: v1
kind: Service
metadata: # kpt-merge: example/echo
  name: echo
  namespace: staging
  labels:
    app: staging
  annotations:
    internal.kpt.dev/upstream-identifier: '|Service|example|echo'
spec:
  ports:
  - port: 80
    targetPort: 5678
  selector:
    app: staging
